import{Link}from"react-router-dom";import{useState}from"react";import{supabase}from"../lib/supabase";export default function Forgot(){const[email,setEmail]=useState(""),[msg,setMsg]=useState(""),[busy,setBusy]=useState(false);async function go(e){e.preventDefault();if(!supabase)return setMsg("Add Supabase keys in .env");setBusy(true);const{error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/reset-password`});setBusy(false);setMsg(error?error.message:"Reset link sent. Check your email.")}return <main className="auth"><form onSubmit={go}><h1>Reels RA</h1><h2>Forgot password?</h2><p>Enter your account email and we'll send a reset link.</p><input placeholder="Email" type="email" value={email} onChange={x=>setEmail(x.target.value)}/>{msg&&<p>{msg}</p>}<button disabled={busy}>{busy?"Sending...":"Send reset link"}</button><Link to="/login">Back to login</Link></form></main>}
