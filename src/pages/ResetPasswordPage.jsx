import{Link,useNavigate}from"react-router-dom";import{useState}from"react";import{supabase}from"../lib/supabase";export default function Reset(){const n=useNavigate(),[password,setPassword]=useState(""),[msg,setMsg]=useState(""),[busy,setBusy]=useState(false);async function go(e){e.preventDefault();if(!supabase)return setMsg("Add Supabase keys in .env");if(password.length<6)return setMsg("Password must be at least 6 characters.");setBusy(true);const{error}=await supabase.auth.updateUser({password});setBusy(false);if(error)return setMsg(error.message);setMsg("Password updated. Redirecting to login...");setTimeout(()=>n("/login"),1500)}return <main className="auth"><form onSubmit={go}><h1>Reels RA</h1><h2>Reset password</h2><p>Open this page from the reset link sent by email, then set a new password.</p><input placeholder="New password" type="password" value={password} onChange={x=>setPassword(x.target.value)}/>{msg&&<p>{msg}</p>}<button disabled={busy}>{busy?"Saving...":"Save new password"}</button><Link to="/login">Back to login</Link></form></main>}
